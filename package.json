{
  "private": true,
  "name": "monorepo",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "precommit": "lerna run --parallel precommit",
    "test": "lerna run --parallel test",
    "format": "lerna run --parallel format"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "lerna": "^3.20.2",
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">= 10.15.2",
    "yarn": ">= 1.13.0"
  }
}
